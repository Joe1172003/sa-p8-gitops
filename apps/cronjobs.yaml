# Los dos cronjobs, bitacora y resumen. Son un solo chart con una sola version
# para las dos imagenes, pero cada uno tiene su propio secreto.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cronjobs
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: sa-p8
  sources:
    - repoURL: https://github.com/Joe1172003/Practicas-SA-B-202200271.git
      targetRevision: f9de49bb8cc3921c74e35f03dbbb652bfc56945a # v2.2.0
      path: P8/charts/cronjobs
      helm:
        releaseName: cronjobs
        valueFiles:
          - values-prod.yaml
          - $gitops/valores/cronjobs.yaml
    - repoURL: https://github.com/Joe1172003/sa-p8-gitops.git
      targetRevision: main
      ref: gitops
    # El secreto cifrado. ArgoCD lo aplica tal cual y el controlador de
    # Sealed Secrets lo abre dentro del cluster.
    - repoURL: https://github.com/Joe1172003/sa-p8-gitops.git
      targetRevision: main
      path: secretos
      directory:
        include: '{bitacora.yaml,resumen.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: sa-p8
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
